#!/bin/bash

# Create site.json

# Usage: site config.json pages.json version  > site.json

VERSION=$1
CONFIG=$2
PAGES=$3

jq  --sort-keys				\
    --from-file ${0}.jq     \
    --slurpfile pages "$PAGES"	\
    --arg version $VERSION  \
    "$CONFIG"

exit

# vim:ts=4:sw=4:ai:et:syntax=sh
